# --- Public site (SEO + sitemap + robots.txt at `vite build`) ---
# No trailing slash. Set on Vercel to your production or preview URL.
VITE_SITE_URL=

# Legacy alias for VITE_SITE_URL only (same value). Prefer VITE_SITE_URL above.
# VITE_SITEMAP=

# --- Contact form (FormSubmit) ---
# Optional. Inbox for contact submissions (default in code: absamaard@gmail.com).
# VITE_CONTACT_EMAIL=
